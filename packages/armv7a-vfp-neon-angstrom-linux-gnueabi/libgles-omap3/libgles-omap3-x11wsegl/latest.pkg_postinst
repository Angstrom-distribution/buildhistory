rm -f $D/etc/powervr-esrev

echo "[default]" > $D/etc/powervr.ini
echo "WindowSystem=libpvrPVR2D_X11WSEGL.so.1" >> $D/etc/powervr.ini
if [ x"$D" = "x" ]; then
	if [ -x /sbin/ldconfig ]; then /sbin/ldconfig ; fi
fi

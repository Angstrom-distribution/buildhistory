#!/bin/sh
OPTS=""

if [ -n "$D" ]; then
    OPTS="--root=$D"
fi

systemctl $OPTS enable bonescript-autorun.service bonescript.service bonescript.socket

if [ -z "$D" -a enable = "enable" ]; then
    systemctl start bonescript-autorun.service bonescript.service bonescript.socket
fi

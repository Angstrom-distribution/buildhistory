	update-alternatives --install /boot/zImage zImage zImage-3.11.0 31111 || true
	if [ ! -e "$D/lib/modules/3.11.0" ]; then
		mkdir -p $D/lib/modules/3.11.0
	fi
	if [ -n "$D" ]; then
		depmodwrapper -a -b $D 3.11.0
	else
		depmod -a 3.11.0
	fi

	update-alternatives --install /boot/bzImage bzImage bzImage-3.8.13-yocto-standard 30813 || true
	if [ ! -e "$D/lib/modules/3.8.13-yocto-standard" ]; then
		mkdir -p $D/lib/modules/3.8.13-yocto-standard
	fi
	if [ -n "$D" ]; then
		depmodwrapper -a -b $D 3.8.13-yocto-standard
	else
		depmod -a 3.8.13-yocto-standard
	fi

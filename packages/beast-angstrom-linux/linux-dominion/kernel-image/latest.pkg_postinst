	update-alternatives --install /boot/bzImage bzImage bzImage-3.13.1 31301 || true
	if [ ! -e "$D/lib/modules/3.13.1" ]; then
		mkdir -p $D/lib/modules/3.13.1
	fi
	if [ -n "$D" ]; then
		depmodwrapper -a -b $D 3.13.1
	else
		depmod -a 3.13.1
	fi

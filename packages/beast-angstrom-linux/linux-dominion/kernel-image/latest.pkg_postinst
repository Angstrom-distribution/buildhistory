	update-alternatives --install /boot/bzImage bzImage bzImage-3.13.4 31304 || true
	if [ ! -e "$D/lib/modules/3.13.4" ]; then
		mkdir -p $D/lib/modules/3.13.4
	fi
	if [ -n "$D" ]; then
		depmodwrapper -a -b $D 3.13.4
	else
		depmod -a 3.13.4
	fi

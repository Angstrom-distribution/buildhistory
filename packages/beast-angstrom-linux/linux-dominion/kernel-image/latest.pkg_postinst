	update-alternatives --install /boot/bzImage bzImage bzImage-3.13.3 31303 || true
	if [ ! -e "$D/lib/modules/3.13.3" ]; then
		mkdir -p $D/lib/modules/3.13.3
	fi
	if [ -n "$D" ]; then
		depmodwrapper -a -b $D 3.13.3
	else
		depmod -a 3.13.3
	fi

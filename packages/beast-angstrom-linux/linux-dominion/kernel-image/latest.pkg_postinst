	update-alternatives --install /boot/bzImage bzImage bzImage-3.13.5 31305 || true
	if [ ! -e "$D/lib/modules/3.13.5" ]; then
		mkdir -p $D/lib/modules/3.13.5
	fi
	if [ -n "$D" ]; then
		depmodwrapper -a -b $D 3.13.5
	else
		depmod -a 3.13.5
	fi

	update-alternatives --install /boot/bzImage bzImage bzImage-3.13.0 31313 || true
	if [ ! -e "$D/lib/modules/3.13.0" ]; then
		mkdir -p $D/lib/modules/3.13.0
	fi
	if [ -n "$D" ]; then
		depmodwrapper -a -b $D 3.13.0
	else
		depmod -a 3.13.0
	fi

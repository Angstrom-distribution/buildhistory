	update-alternatives --install /boot/bzImage bzImage bzImage-3.13.2 31302 || true
	if [ ! -e "$D/lib/modules/3.13.2" ]; then
		mkdir -p $D/lib/modules/3.13.2
	fi
	if [ -n "$D" ]; then
		depmodwrapper -a -b $D 3.13.2
	else
		depmod -a 3.13.2
	fi

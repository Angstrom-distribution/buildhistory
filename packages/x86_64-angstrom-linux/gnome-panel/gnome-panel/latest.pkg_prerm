#!/bin/sh
SCHEMA_LOCATION=/etc/gconf/schemas
for SCHEMA in window-list.schemas panel-general.schemas clock.schemas panel-global.schemas panel-compatibility.schemas panel-toplevel.schemas fish.schemas panel-object.schemas workspace-switcher.schemas; do
	if [ -e $SCHEMA_LOCATION/$SCHEMA ]; then
		HOME=/root GCONF_CONFIG_SOURCE=`gconftool-2 --get-default-source` \
			gconftool-2 \
			--makefile-uninstall-rule $SCHEMA_LOCATION/$SCHEMA > /dev/null
	fi
done

	if [ "x$D" != "x" ]; then
		exit 1
	fi

	test -x /usr/bin/glib-compile-schemas && glib-compile-schemas  /usr/share/glib-2.0/schemas
if [ x"$D" = "x" ]; then
	if [ -x /sbin/ldconfig ]; then /sbin/ldconfig ; fi
fi

#!/bin/sh
SCHEMA_LOCATION=/etc/gconf/schemas
for SCHEMA in panel-toplevel.schemas panel-global.schemas panel-object.schemas panel-compatibility.schemas panel-general.schemas fish.schemas clock.schemas window-list.schemas workspace-switcher.schemas; do
	if [ -e $SCHEMA_LOCATION/$SCHEMA ]; then
		HOME=/root GCONF_CONFIG_SOURCE=`gconftool-2 --get-default-source` \
			gconftool-2 \
			--makefile-uninstall-rule $SCHEMA_LOCATION/$SCHEMA > /dev/null
	fi
done

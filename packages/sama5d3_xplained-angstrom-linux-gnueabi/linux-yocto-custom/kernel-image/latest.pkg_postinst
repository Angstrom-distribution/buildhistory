	update-alternatives --install /boot/zImage zImage zImage-3.10.0-custom 31010 || true
	if [ ! -e "$D/lib/modules/3.10.0-custom" ]; then
		mkdir -p $D/lib/modules/3.10.0-custom
	fi
	if [ -n "$D" ]; then
		depmodwrapper -a -b $D 3.10.0-custom
	else
		depmod -a 3.10.0-custom
	fi

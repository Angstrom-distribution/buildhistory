#!/bin/sh
OPTS=""

if [ -n "$D" ]; then
    OPTS="--root=$D"
fi

systemctl $OPTS enable storage-gadget-init.service

if [ -z "$D" -a enable = "enable" ]; then
    systemctl start storage-gadget-init.service
fi
